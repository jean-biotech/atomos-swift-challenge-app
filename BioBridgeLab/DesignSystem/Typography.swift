import SwiftUI

extension Font {
    static let appTitle: Font       = .system(.title2)
    static let appSubtitle: Font    = .system(.title3)
    static let appBody: Font        = .system(.body)
    static let appCaption: Font     = .system(.subheadline)
    static let appLargeTitle: Font  = .system(.title)
    static let appHero: Font        = .system(.largeTitle)
}
